apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glover-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "glover-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glover-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
